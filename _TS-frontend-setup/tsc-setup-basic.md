# TS frontend project setup

## Init

```sh
npm init -y
```

add npm commend

```json
    "dev": "tsc -w",
    "start": "lite-server"
```

create tsconfig.json

Install ts
```
    pnpm i -D typescript
```